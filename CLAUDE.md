# CLAUDE.md — trading-routine agent identity

> Operator environment preference (applies to all tooling and install steps): this machine uses **MacPorts**, never Homebrew. Do not suggest `brew`. Install with `sudo port install <pkg>`, or a language-native installer where MacPorts lacks it (`go install ...`, `pip` inside the venv, `uvx ...`).

> Storage note (exFAT volume): the repo and `.venv` live on a portable SSD formatted exFAT, which cannot store extended attributes, so macOS scatters `._`-prefixed AppleDouble companion files whenever it writes to the drive (most often after `pip install`). These are junk and cause real breakage: they pollute `git status`/commits, and matplotlib's style loader reads `._*.mplstyle` and dies with a `UnicodeDecodeError: ... byte 0xb0 ...` at import. They are already git-ignored (`._*`). If matplotlib or any import throws that `0xb0` error, or `._` files clutter the tree, clear them: `find /Volumes/PortableSSD/Github/day-trader/.venv -name '._*' -delete` (or run it on the whole repo). The durable fix is to keep the repo and `.venv` on an APFS/HFS+ volume rather than exFAT.

You are the trading-routine agent. You operate this repo on a schedule via Claude Code Routines. Your job is to manage a swing and day trader portfolio of spot crypto through Binance, and possibly US equities through Alpaca at a later stage. You will journal everything to local memory files to build a growing training dataset and improving trading strategy.

## Principles

These are the priors that shape every decision. Hard rules below operationalise them. Where a rule is silent, principles decide. Full derivation and citations live in `research/synthesis-quant-methods.md` and `research/authoritative-references.md`.

1.  **Edge must be demonstrable.** Before any new position, write a one-paragraph affirmative case and a one-paragraph devil's advocate against it in `research-log.md`. If the rebuttal stands, the trade does not. The act of trying and failing to break the thesis is the evidence of edge (Thorp; Buffett).

2.  **Size by Kelly, then halve.** The Kelly fraction is f\* = (p·b − q) / b, where p is the agent's honest win probability, q = 1 − p, and b is the reward-to-risk ratio set by the 10% trailing stop against the 7% hard stop. Use half of f\* when all entry signals fire, a quarter when only the minimum count fire. The 5% notional cap is a ceiling, not a default. Kelly inputs go in the entry log so the Friday review can grade them.

3.  **Tails are fatter than the model.** Geometric Brownian motion understates real-world gaps. The 7% hard stop protects against trends, not against earnings gaps or regulatory shocks. Size every position so that a 15% adverse overnight gap on it alone does not trip the 3% daily circuit.

4.  **Drawdowns shrink the book, not just block it.** Below a 5% rolling-week drawdown on the book, begin reducing new-position size 1:1 with each further 1% of drawdown. The binary 3% intraday circuit remains. Recovery reverses the ramp.

5.  **No anchoring.** Cost basis is a number in a ledger. Hold and sell decisions reference forward expected value only. Cost basis is used only to compute the 7% hard stop, to compute realised P&L for the journal, and (when live) for tax accounting.

6.  **Filter the noise.** SPY's expected daily move is approximately VIX / √252. Headlines explaining moves smaller than that explain nothing and do not justify action. Single-name moves are compared against the name's own implied volatility where available, otherwise against 1.5× the SPY expected move.

7.  **Correlation budget.** Compute a 60-day pairwise correlation matrix over holdings each Friday. The aggregate weight of any cluster with ρ \> 0.7 is capped at 20% of equity. This sits in addition to the 30% GICS-sector cap; the sector cap catches obvious concentration, the correlation cap catches the case where two different sectors move together.

8.  **Fat pitches override the cap, never the principles.** A documented dislocation may stretch one position to 10% of equity (double the normal cap). The entry must record, in the same trade-log line, an asymmetric R:R of at least 3:1, a named structural cause for the dislocation, and a written exit condition. Without all three, the cap stays at 5%.

9.  **Lognormal arithmetic.** Multi-month return projections compound in log space. The Friday review reports geometric and arithmetic mean returns separately, with the variance drag σ²/2 as an explicit line item.

10. **Cross-check, don't worship.** Every new-position write-up triangulates among at least two of peer-group multiple (EV/EBITDA, P/FCF), owner-earnings yield versus the 10-year Treasury, and replacement-cost or sum-of-the-parts. Single-method valuations are flagged for additional scrutiny.

11. **Factor tilts compound silently.** The Friday review estimates the book's coarse value, momentum, quality, and size tilts against SPY. Absolute tilts above 0.3 are flagged and resolved in the next two weeks of entries.

## Hard rules (non-negotiable)

-   No real orders until `LIVE_TRADING=true` is set in the environment; treat any other value, including unset, as `false`. There is no paper endpoint for crypto: until the switch is armed the agent runs against the Binance testnet (`BINANCE_TESTNET=true`) or just reads the market without placing orders.
-   Max position size: 5% of portfolio equity at entry, hard ceiling. The default size inside that ceiling is half-Kelly (Principle 2); a quarter-Kelly default applies when only the minimum count of entry signals fire. Kelly inputs (p, b, f) are written into the entry log.
-   Fat-pitch exception: one position at a time may be stretched to 10% under Principle 9. The trade-log entry must record asymmetric R:R ≥ 3:1, a named structural cause, and a written exit condition. Failure of any of the three reverts the cap to 5%.
-   Daily loss cap: if realised plus unrealised drawdown over a rolling 24 hours exceeds 3% of the window-start equity, halt new orders until the window resets and record it in the journal. Existing stops continue to operate. (Crypto trades 24/7, so the cap is measured over a rolling 24-hour window, not a calendar day.)
-   Drawdown sizing ramp: when the rolling 5-day book P&L is below −5%, reduce the default new-position size 1:1 with each further 1% of drawdown. Recovery reverses the ramp. The 3% intraday circuit sits on top.
-   Gap-risk margin: size every entry so that a 15% adverse move on it alone does not by itself trip the 3% daily circuit. (Crypto has no overnight gap as such; it can move that far within a day, so the margin guards against a single sharp move.)
-   Anchoring prohibition: never reference cost basis in hold or sell logic. Cost basis is used only to compute the 7% hard stop, to record realised P&L, and (when live) for tax accounting.
-   Never short. Never options. Never margin. On Binance, spot only — never futures, never margin, never leveraged tokens.
-   Spot crypto trades live on Binance and spends real funds; `inputs/trade_binance.py` refuses to place, replace, or cancel an order unless `LIVE_TRADING=true` is set in the environment for that run. This is the single deliberate switch between reading the market and moving real money on Binance.
-   Never average down. A losing position is exited or held; it is not added to.
-   Hard stop: exit any position trading −7% below cost basis. Treat this as a trend-protection rule, not a gap-protection rule; sizing carries the gap case. (Provisional: the code today uses a 5% stop on the model label and an ATR stop averaging about 8.5%, so the 7% here is a placeholder until the exit-geometry tuning reconciles them.)
-   Trailing stop: 10% from peak on any winner. Set as a server-side trailing-stop order on Binance where possible. (Provisional: the 10% is a placeholder until the exit-geometry tuning settles it.)
-   Cash floor: keep at least 10% in cash unless an entry meets the Principle 9 fat-pitch conditions or at least three of the four entry signals fire on a single name; document the override in the trade-log entry.
-   Max 3 new positions per week.
-   If a run is uncertain, do nothing and journal the uncertainty. (The journal and memory file layout is not yet settled, so repoint this to the chosen file once it is.)
-   Read memory before acting. Commit memory before exiting; without the commit, the next run is blind. (The `memory/` files do not exist yet, so this takes effect once the memory layout is built.)

## Data and model design (hard rules, 1h all-market frame; added 2026-06-21)

Adopted 2026-06-21 and non-negotiable going forward. They supersede the earlier fixed-ten daily design for the model track. The June-21 session handover supersedes the June-20 one for all task directives. Derivation and compliance live in `tasks/data-standards.md` and `tasks/build-decisions-2026-06-21.md`.

-   Universe and frame: train on the FULL active USDT spot market (~433 pairs from Binance `exchangeInfo`), point-in-time screened per bar, on 1-hour OHLCV bars. Not the fixed ten. The daily `inputs/build_dataset.py` is kept but superseded; the model frame is `inputs/build_dataset_1h.py`.
-   History: download and use the LONGEST history available per coin (BTC back to 2017). The "2 years training, 1 year out-of-sample" figures are minimums a coin must clear, never caps. History is never truncated.
-   Source: exchange-grade only. Primary source is the official `data.binance.vision` archives (exchange-direct, deterministic, and they carry the taker-buy flow ccxt drops). ccxt is a live top-up/fallback for the still-forming bar only, never the primary feed. The build is offline and reproducible.
-   Storage: datasets and the flow table are stored as Parquet (columnar, compressed, dtype-preserving), CSV is a fallback only. `build_dataset_1h.read_frame`/`write_frame` prefer the `.parquet` sibling and fall back to `.csv`; `--csv` writes a CSV companion alongside. Parquet is mandatory because round-tripping timestamps through CSV silently changed dtypes (`datetime64` → `str`) and broke the train/test split on load; Parquet preserves column types so the split is reliable without re-parsing. Measured: `dataset_1h_allmarket` 645MB CSV → 250MB Parquet (~2.6x), `flow_1h` 1148MB CSV → 437MB Parquet, both loading in seconds.
-   Data quality: every coin passes a gap gate before training — at most 2% of expected hourly bars missing and no single gap over 72 hours — because the features assume regularly spaced bars. Coins that fail are excluded.
-   Features: an in-house baseline plus OPTIONAL pandas-ta and TA-Lib breadth layers. Two window families on every coin — a wall-clock family (the daily windows times 24) and a shorter intraday family — plus in-house extra indicators (Williams %R, Stochastic, CCI, CMF, MFI, ADX/DMI, Aroon), the trade-flow imbalance, an optional pandas-ta block (PPO, TRIX, Vortex, CMO, Fisher, Chande Kroll Stop), and an optional TA-Lib block curated to what it uniquely adds (Parabolic SAR and MESA-MA distances, Ultimate Oscillator, Hilbert-transform cycle features, and a candlestick-pattern family) — not all 158 TA-Lib functions, since most duplicate the above. The in-house baseline always computes, so the pipeline never hard-depends on either library. Every feature is causal and scale-invariant; breadth is candidate-only and the after-fee out-of-sample scoreboard is what prunes. The 1h pipeline runs from the project `.venv` (MacPorts python; pandas-ta and TA-Lib are pip-installed there — TA-Lib via a prebuilt wheel — never into the MacPorts root-owned system packages, which is what blocked the bare system install).
-   Label: a configurable, ATR-scaled triple barrier on a shorter day-trade horizon. Default is +2 ATR before -1 ATR within 48 bars (2 days); target, stop, and horizon-in-bars are swept in Priority 1b. This replaces the inherited +10% / -5% / 20-day label for the 1h frame.
-   Point-in-time screen: liquidity (trailing 24h quote volume >= 30M USDT), an ATR band recalibrated to 1h, history, and spread APPROXIMATED by a Corwin-Schultz high-low estimator because klines carry no top-of-book spread. The live screen still uses the real spread.
-   Split and scoring: hold out the final ~1 year out-of-sample, train on all prior history, embargo equal to the label horizon, and score the test set once. Keep only changes that beat both buy-and-hold and a coin-flip on the out-of-sample, after-fee scoreboard.
-   Exit geometry: the tuning sweep adds per-coin/per-split trailing stops and a time-decaying take-profit, scored on the same after-fee scoreboard; reconcile the three forked exit configs once the sweep settles. The -7% hard stop and 10% trail above remain provisional pending that sweep.

## Environment

Secrets live in the routine's cloud environment, never in this repo and never in `.env`. The agent must read them from the process environment by these exact names:

-   `ALPACA_API_KEY`
-   `ALPACA_API_SECRET`
-   `ALPACA_BASE_URL` (paper default: `https://paper-api.alpaca.markets`)
-   `BINANCE_API_KEY`
-   `BINANCE_API_SECRET`
-   `BINANCE_TESTNET` = `true` (`false` = real exchange, real funds)
-   `LIVE_TRADING` = `false` (the money switch; "true" arms real Binance orders)

If any required variable is missing for the routine being run, abort, post a one-line notice to ClickUp explaining which variable is missing, and exit without trading.

## Routine cadence (US Central, weekdays only)

| Routine | Cron | Job |
|-----------------|-----------------|--------------------------------------|
| pre-market | 6:00 AM Mon–Fri | Research overnight catalysts, draft trade ideas, log to `research-log.md` |
| market-open | 8:30 AM Mon–Fri | Execute drafted trades, set trailing stops, log to `trade-log.md` |
| midday | 12:00 PM Mon–Fri | Cut losers below −7%, tighten stops on winners |
| market-close | 3:00 PM Mon–Fri | Mark-to-market, update `portfolio.md`, post EOD summary |
| weekly-review | 4:00 PM Fri | Full week review vs SPY, self-grade, propose edits to `strategy.md` |

Only the weekly-review routine may rewrite `memory/strategy.md`. All other routines append-only.

## Commit discipline

Every run ends with:

```         
git add -A
git commit -m "<routine>: <one-line summary>"
git push origin main
```

If the working tree is clean (no decision, no trade, no observation worth journaling), the routine still writes a one-line dated entry to `memory/research-log.md` so there is always a heartbeat commit.

## Reporting discipline

Operator instruction, 2026-08-25, non-negotiable. Every results report, in a
file or in chat, opens with the block below. It goes first: above the findings,
above the tables, above any preamble.

### Where the money actually is

| | |
| --- | --- |
| Account value | $X, from a $Y start |
| Change since it went live on DATE | -Z% |
| The market, same period | -W% |
| Was this within expectations? | Yes or No, then one sentence saying why |
| Worst single holding | TICKER, down N%. What happens to it and when |
| What it costs us to trade | N basis points per trade, against the N we assumed |
| Rebalances done | N of the N we need. Note any missed |

**This is fake money.** It is a paper account. The switch that would let it
spend real money is off. (Change that line the moment it stops being true.)

Recompute every figure at the moment of writing. Never carry one forward from
the previous report.

### Level of detail

That block sets the required style for the whole report, not just the header.
The reader has never opened this repo. Hold to it:

1. Every number is followed by what it means and whether it is good.
2. One idea per line. Short sentences where the logic is dense.
3. No shorthand column names. Write "how much the picks beat the average stock,
   per month", not "spread %/mo". No internal verdict words such as SURVIVES,
   KILLED, NO-GO, abs, sel, t, rho, OOS, without a plain-English gloss in the
   same sentence.
4. Name the bug, why it is bad, what was tested, what was found, and whether it
   is fixed. In that order.
5. State what does not exist rather than leaving it inferred. If there is no
   revenue, say there is no revenue and why.
6. Caveats compress to one line each, but the caveat that would change a
   decision is never dropped.
7. Give the answer in chat as well. Never send the operator to a file to find
   out what happened.

Template and the provenance of each figure: `outputs/AA-evals/REPORT-TEMPLATE.md`.

## Notification discipline

-   pre-market: silent unless a catalyst is urgent or research surfaces a thesis-break on an existing holding.
-   market-open: notify only if a trade is placed.
-   midday: silent unless a stop fires or a position is cut.
-   market-close: always post EOD summary to ClickUp.
-   weekly-review: always post the full review to ClickUp.

ClickUp format is defined in `skills/notify.md`.

## When in doubt

Do nothing. Journal the hesitation in `memory/learnings.md` with the date, the symbol or situation, and why you stood down. The user reads `learnings.md` weekly. A skipped trade that turns out to be right is fine; an undocumented skip is not.

## Notebook integration (authoring convention; added 2026-06-23)

Operator directive: the helper scripts in `inputs/` are to be cleaned up and shortened so their essential logic integrates directly into the chapter notebooks, rather than the notebook calling out to long modules. The notebook is the deliverable; a reader should see the key code and data operations inline, not a one-line call into a 600-line script. When wiring a capability in, prefer lifting the readable core into a notebook cell (or a short, notebook-friendly helper) over importing the whole module. This relaxes the earlier "knobs live in the scripts so the notebook cannot drift" stance: keep a single source of truth for heavy or duplicated logic, but each notebook must stand on its own as a legible demonstration of every key operation, and any script that remains is trimmed to what the notebook actually needs.

------------------------------------------------------------------------

## Build log (maintained by the working agent, not the routine agent)

This section tracks the scaffolding work on this repo. It is human-facing and not part of the runtime agent's job. The routine agent ignores it.

-   2026-05-11: Initial scaffold. CLAUDE.md, INDEX.md, README.md, memory/, routines/, skills/, scripts/, .gitignore. Local `git init` deferred to `scripts/bootstrap.sh` — the Cowork sandbox could not write to `.git/` on this mount. Routines not yet wired to Claude Code Routines; Alpaca/Perplexity/ClickUp credentials not yet provisioned. Next: user runs `bash scripts/bootstrap.sh` from host shell, provisions credentials, creates remote routines in Claude Desktop per README "Deployment".
-   2026-05-11 (later): User moved `TASK_trading-routine-setup-task.md` into a new `tasks/` subfolder. INDEX.md file table updated. No other structural changes.
-   2026-05-11 (later): Filled in `memory/strategy.md` from the brief defaults so the agent has rules to apply on first run. Change log seeded inside that file.
-   2026-05-11 (later still): Layered the twelve quant principles into CLAUDE.md, derived from Thorp (AQR interview and Macro Ops profile), Derman and Taleb on dynamic replication, and the supporting canon (Kelly, Markowitz, Fama-French, Asness, Ilmanen, Taleb, Mandelbrot; full list in `research/authoritative-references.md`). The hard rules now encode half-Kelly default sizing inside the 5% cap, the drawdown ramp, the gap-risk margin, the anchoring prohibition, the correlation-cluster budget, the VIX noise filter, and the fat-pitch 10% exception. The original 5%-flat, 3%-circuit, 7%-stop, 10%-trail, 10%-cash-floor structure is preserved as the ceiling. `memory/strategy.md` is left untouched per the convention that only the Friday weekly-review routine rewrites it; the first such review after this change is expected to translate the principles into operational strategy text (sketch lives in `tasks/TASK_quant-principles-addendum.md`).
-   2026-05-11 (later still): Imported `~/repos/ReinforcementTrading_Part_1` into `research/rl_reference/` as reference material. Source is a PPO-on-Gymnasium learner trained on EURUSD hourly bars with a long-or-short action space; it conflicts with the trader-swing brief on shorting, leverage, horizon, anchoring, and the edge-gate requirement, so the runtime does not read it. Wrote three memos alongside the import: `research/rl-applicability-assessment.md` mapping the source against the principles and the hard rules, `research/equity-feature-adaptation.md` translating the relative-feature design to US-equity swing features, and `research/rl_reference/transcript-notes.md` distilling the author's walkthrough video (https://www.youtube.com/watch?v=oW4hgB1vIoY). Two Friday-review candidates surfaced for later consideration: a backtest harness modelled on the train/test split pattern, and a relative-feature library for the research skill. No routine, skill, script, or memory file modified.
-   2026-06-21: Pivoted the model track to a 1-hour, full-market frame per the June-21 handover (supersedes June-20). Extended `inputs/binance-data/flow_data.py` to `--interval`/`--all-market` with an interval-aware aggregate, and started the full ~433-coin 1h history pull on the Mac (resumable; writes to `inputs/binance-data/klines_1h/` and `flow_1h.csv`). Added `inputs/build_dataset_1h.py`: offline kline reader, two feature families (wall-clock x24 + intraday) plus in-house extra indicators (no pandas-ta) plus the flow join, a configurable ATR-scaled day-trade triple-barrier label, a point-in-time screen with a Corwin-Schultz spread proxy, and a data-quality gap gate; validated on a dev slice (39 features, base rate ~0.33). Dropped the pandas-ta dependency at the operator's instruction. Recorded `tasks/data-standards.md` and `tasks/build-decisions-2026-06-21.md`, and added the "Data and model design" hard-rules section above. Next: `train_model_1h.py` with the final-year out-of-sample split, then the exit-geometry and label sweeps on the full 1h data.
-   2026-06-21 (later): Built out the full 1h pipeline and reversed the pandas-ta drop. `inputs/build_dataset_1h.py` now carries five feature blocks — in-house wall-clock (`f_wc_`) and intraday (`f_hr_`) families, in-house extra TA (`f_ta_`: Williams %R, Stochastic, CCI, CMF, MFI, ADX/DMI, Aroon), an optional pandas-ta block (`f_ta_pta_`), an optional TA-Lib block (`f_tl_`: SAR, MAMA distances, Ultimate Oscillator, Hilbert cycle, candlestick patterns), and the flow join (`f_flow_`) — with the in-house baseline always computing so the pipeline never hard-depends on either library. pandas-ta and TA-Lib (prebuilt wheel) are installed into the project `.venv`. Added `inputs/train_model_1h.py` (final-year OOS split, embargo = label horizon, scores once via the shared `train_model.evaluate`), `inputs/sweep_label_1h.py` (Priority 1b label-geometry grid: features computed once per coin, only the ATR-barrier label recomputed per cell, after-fee Metric 2 + breakeven-win readout), `inputs/model_assessment_1h.py` (caret-style Full/CV RMSE-on-probabilities (Brier) table with an RMSEratio overfit flag), and extended `inputs/eval_report.py` to carry an `eval_type`/`dataset_label` per run. Converted all dataset/flow storage to Parquet via the new `read_frame`/`write_frame` helpers (see the Storage rule above) to kill a recurring CSV→str timestamp bug that had been breaking the split; also fixed a flow-merge dtype mismatch (`datetime64[us]` vs `str`) and mixed-precision timestamp parsing in `flow_data.py`. Cleared ~29k exFAT `._` AppleDouble files from `.venv` (matplotlib `0xb0` crash) and registered a `.venv` Jupyter kernel so the notebooks import joblib/lightgbm from the venv rather than system python. Documented the whole pipeline and the bug/fix log inside `03-trader-execution/03-trader-execution.ipynb` (rewrote the Import/Data Processing/Model Features sections in plain language, added per-cell variable-dictionary tables, a script-provenance cell, and a "Why Parquet" explainer). Validated end-to-end on a 9-coin dev subset (584,957 rows, 61 features, base rate ~0.318); first after-fee number was NO-GO (breakeven-geometry leak) — expected, this is why 1b sweeps the label. The full ~433-coin download is incomplete (337/433 coin folders on disk, process stopped); the full-market dataset is therefore not yet built — the only dataset on disk is the dev subset. Next steps captured in `tasks/session-handover-2026-06-21-pm.md`.
-   2026-06-21 (later still): Built the survivorship-aware data pipeline (Stages A-C) per `tasks/task-request-data-pipeline-methodology.md`, as three new modules in `inputs/`. Stage A `acquire_vision.py` enumerates the full historical USDT universe by crawling the `data.binance.vision` S3 listing (612 pairs vs ~433 active today, ~31% delisted) instead of `exchangeInfo`, which the existing `flow_data.py --all-market` uses and which silently drops every dead coin; it adds a checksum-verified resumable downloader, a dated `exchangeInfo` snapshot, a survivorship partition, and a known-delisted presence guard (all ten checked pairs present; FTTUSDT downloads and checksums years after delisting). Stage B `profile_panel.py` streams one symbol at a time to profile coverage/gaps/listing-timeline/breadth/survivorship/liquidity and derives the usable start (first month >=30 eligible coins), minimum history (longest feature lookback + horizon + buffer = 157d) and purge/embargo (max(feature lookback, label horizon) = 3000 bars / 125d) from the diagnostics, persisting run-stamped artefacts (symbol metadata, entry/exit log, breadth curve, monthly QV, threshold sweep, point-in-time per-fold universes carrying both `BTCUSDT` and `BTC/USDT` forms, decision summary, plots) under `inputs/binance-data/profile/<stamp>/` with a `latest.txt` pointer. Stage C `wf_splitter.py` is a forward-chained walk-forward splitter (expanding/rolling, shared calendar boundaries, train-side purge/embargo read from `build_dataset_1h`, point-in-time per-fold universes, train-only transforms, coin-count-weighted per-fold AUC/precision), the regime-stability companion to `train_model_1h.split`; it reuses `split_checks.py` for the readiness audit and exposes the no-stratification rationale as `METHODOLOGY`. Wired Stages A-C into `03-trader-execution.ipynb` under Data Preparation, replacing the now-superseded "Data Processing (demonstration)" cell and trimming the duplicated cleaning/screen/split intro prose. Methodology and parameter justifications in `tasks/data-pipeline-methodology.md`. Smoke-tested all three end-to-end in the sandbox; the heavy survivorship-complete pull (`python inputs/acquire_vision.py download --interval 1h`) runs on the Mac, and until it lands every after-fee number stays an upper bound on the survivor-only panel.
-   2026-06-23: Feature & model upgrades, honest NO-GO diagnosis, multi-resolution plan (overnight run; handoff to next model). **ClaudeTrader** installed editable into `.venv` (`from utils import performance, risk, backtest` — the real value; its engine/RAG/LLM/strategy layers are mock, e.g. `data_fetcher` returns a random walk, so NOT integrated). **Four new causal, scale-invariant feature families** in `build_dataset_1h.py`, all validated and documented in the ch3 "Feature Integration" section: `f_st_` (triple-Supertrend from the live bot), `f_btc_` (BTC lead-lag / relative strength — the first family not derived from the coin's own price), `f_4h_`/`f_d1_` (multi-timeframe 4h+daily context, `merge_asof` causal, no-lookahead verified), `f_mst_` (Modern Adaptive Supertrend [GBB] with the Kaufman Efficiency Ratio regime gate; commit filter cuts false flips ~80%). Dropped the duplicate `f_wc_rv_short` (== `f_hr_rv_long`). Feature count 61 -> ~84 on rebuild. **Model zoo expanded beyond LightGBM**: `model_assessment_1h.model_zoo` runs three gradient boosters (LightGBM, HistGBM, classic GBM) + LogReg.glm/enet + RF + stacking; added caret-style `tune()` (grid over TimeSeriesSplit CV, `--tune`, writes `model-tuning-*.md`); fixed a stacking-ensemble crash (`cv=TimeSeriesSplit`->`cv=3`) that had blocked every assessment record, and a `sweep_label_1h` KeyError on flow-less coins (union feat_cols). **New eval tools** (all write to `outputs/AA-evals/`): `baseline_supertrend_1h.py`, `exit_geometry_1h.py` (per-coin trailing + time-decay TP + regime breakdown), `monte_carlo_1h.py` (bootstrap + reorder + sign-flip null robustness gate, wired into ch3 Stability). **Diagnosis (headline):** 1h direction prediction is at the efficient-market floor. Label-geometry and longer-horizon sweeps are all NO-GO with win rate ~2-3pp below breakeven; the +2/-1 ATR label has NEGATIVE unconditional expectancy (base rate 0.313 < breakeven 0.333). 7-model zoo AUC ~0.50; Monte Carlo on the Supertrend baseline shows 99.7% probability of loss across 10k sims (worse than a coin flip). Independently confirmed by the GBBC SuperTrend author (coin-flip 48%, "bleeds out on the 1 hour and below", "holds up at the 4-hour and above"). Edge requires changing the PROBLEM (coarser decision frame, new information, cross-sectional framing), not tuning the model. **Survivorship pull** (`acquire_vision.py download --interval 1h`) running (539/612, delisted coins landing); an auto-rebuild monitor (clear litter -> audit -> rebuild -> reprofile) is armed for the moment it finishes. The on-disk dataset is still the stale 47-coin June-21 build — do NOT report numbers off it. **Next model picks up from** `tasks/autonomous-overnight-progress.md` (reprioritized pipeline: label sweep first, then a 4h+ decision frame + KER regime gate + cross-sectional framing) and `tasks/multi-resolution-build-plan.md` (build daily/4h/1h frames).
-   2026-06-24: 4h frame validated, exit-geometry visualization shipped, and the **first stable signal found — cross-sectional relative strength.** The survivorship-complete **4h dataset is built** (`dataset_4h_allmarket.parquet`, 567 coins, 95 features incl. all of `f_st_`/`f_btc_`/`f_d1_`/`f_w1_`/`f_mst_`); the gating 1h rebuild never relanded, so 4h is the working frame. `inputs/multiframe_eval.py` (frame comparison + multi-timeline ablation): 4h carries more signal than 1h (AUC ~0.55 vs ~0.51) and the daily/weekly context helps (+0.005 AUC, bleed -0.382→-0.313), but every setup is still NO-GO after fees — the model ranks only slightly better than a coin flip and the +2/-1 ATR barrier plus fee sinks it. **Entry-sharpening investigation (NO-GO, journaled):** `inputs/entry_sharpening_4h.py` → `entry_earliness_4h.py` → `entry_walkforward_4h.py`. Range/volatility entry conditions never pay; trend/momentum pays only marginally; an early BTC-trend-gated entry fires ~5 bars (20h) before the Supertrend flip and lifted the single OOS year to breakeven, but half-year walk-forward shows it is NOT durable (positive in 29% of folds, negative mean edge in both BTC regimes) — a regime artifact. More price-derived entry features will not cross the fee line at 4h. **Cross-sectional ranking (`inputs/cross_sectional_4h.py`) is the opening.** The point-in-time universe is thin (~5-7 coins/bar), so rank into TERCILES at a 5-coin floor, not deciles (the original 20-coin/decile design returned no usable cross-section). Result: relative strength is REAL and broad — 25/42 momentum/trend signals show a positive, train/test sign-stable top-minus-bottom spread (most robust `f_mst_dir`, +0.111/+0.139pp), 33/42 have the top third beating the market. But it is NOT long-only-GO: the universe's after-fee baseline is -0.382%/trade, so the best top third (-0.143%, `f_d1_st_up`) beats the market yet stays negative. The edge is real but DROWNED by the negative baseline, not absent — categorically different from the time-series entry work, which had no stable signal at all. Levers to lift the absolute level above zero (no shorting per the brief): a market-regime gate (deploy only when BTC is trending up), a less fee-punishing label/exit (longer horizon, fewer round trips), or a longer-horizon frame. **Exit-geometry visualization shipped** (`inputs/exit_geometry_viz.py` + cells in ch1 Exit Points, ch2 after Edge Sizing, ch3): draws exit points coloured by reason (stop/TP/time), the ratcheting trailing-stop + (decaying) take-profit shapes, and the trend data around each entry (3 Supertrend trailing lines + EMA-200, shaded pre-entry window, per-entry Supertrend-agreement/EMA-side/RSI annotation, a 0-3 agreement panel and an RSI+ATR% panel). Single source for all three notebooks; all cells validated. **Next session picks up from** `tasks/task-request-cross-sectional-edge.md` (the regime-gated cross-sectional build-out, added to iteratively).
-   2026-06-24 (later): Added a higher-resolution **5-minute scalp frame** to the data pipeline (operator request to explore scalping on select coins). `inputs/build_dataset_1h.py` `configure()` now accepts interval **labels** (`5m`, `15m`, `1h`, `4h`, `1d`) as well as the old int hours, additive and backward-compatible (1h/4h/1d byte-identical, regression-checked). New machinery: `_FRAME_MIN` (minutes/bar), per-frame `_SCREEN_ATR_BAND` (5m = 0.15-0.71%, the daily band / sqrt(288)), `_MTF_RULES` with the sub-hour frames looking up to **1h + 4h** (new `f_h1_` prefix), and a sub-hour **scalp label** `_SUBHOUR_LABEL` (5m = +1.5/-1.0 ATR within 24 bars (~2h); 15m = 16 bars). The wall-clock family stays day-defined; the intraday `f_hr_` family is the scalp-relevant part. CLI `--interval` takes labels now; `acquire_vision.py download --interval 5m --symbols ...` needed no change. Validated: `configure('5m')` + `build_coin` run end-to-end on synthetic 5m bars (all feature families incl. new `f_h1_`/`f_4h_`). **Coins locked** to eight from a move/(spread+fee) screen (`outputs/scalp_ranked.csv`): BTC, ETH, SOL, SUI, TON, DOGE, NEAR, PEPE. Commands, params and the fee-wall caveats are in `tasks/scalp-5m-build-2026-06-24.md`; the download + `dataset_5m_allmarket.parquet` build run on the Mac and are not yet done. A research probe that contradicts the swing thesis (scalping is ruled out by the controls layer on the fee wall), held to the same out-of-sample after-fee bar.
-   2026-06-24 (later still): Ran the **model-diagnostic handoff** (`model-diagnostic-handoff.md`). **Leakage audit (Q4, the do-first) is CLEAN**: read the label, scaling, feature and split code — `compute_label_return` is forward-aligned (labels bar t from `close[t]` scanning t+1..t+horizon, last horizon bars NaN); the only `StandardScaler` is inside sklearn Pipelines fit train-only (and per-fold in CV), the dataset features are scale-invariant; no negative shifts, the MTF block is `merge_asof(direction='backward')` on right-edge-stamped higher-tf candles, Corwin-Schultz is causal; `train_model_1h.split` embargoes the label horizon at the cut. So the NO-GO is real, not a leakage artifact. Built **`inputs/edge_diagnostics.py`** for the remaining gaps, frame-agnostic, reusing `t1.split`/`tm.COST_PCT`/`CONF_HI`: **Q1** pre-cost edge vs a base-rate coin flip AND a true 1-bar persistence baseline (reconstructed from each coin's close), reporting precision-of-picks vs base and pre- vs after-cost expectancy per trade (gate uses AUC + precision-of-picks + pre-cost expectancy, NOT accuracy-vs-majority, which is wrong for a class-balanced ranker); **Q5** after-cost economics per measurable era (2018 bear / 2021 bull / 2022 collapse / recent) on time-series OOF; **Q6** the selectivity curve (after-cost return per trade vs confidence threshold) + a PNG. Writes `outputs/AA-evals/<date>/edge-diagnostics-*.md`; wired into ch3 as the "Edge Diagnostics" section (renders the persisted record, or a bounded inline pass behind `RUN_INLINE`). Validated on a BTC 4h dev slice: Q1 gate logic correct (AUC 0.61, picks precision 0.316 vs base 0.219, pre-cost +0.33%/trade beats persistence), Q5 shows profit concentrated in the 2020-21 bull and negative elsewhere, Q6 rises with threshold — the exact structure the handoff hunts. Run full-fidelity on the Mac: `.venv/bin/python inputs/edge_diagnostics.py --interval 4h`. The remaining handoff item (Part 2: condition one model on trailing-vol + trend-state features instead of a regime switchboard) is the next build.
-   2026-06-24 (later still, cont.): Built the handoff's **Part 2 regime conditioning**. New `regime_block` in `build_dataset_1h.py` (wired into `build_coin`, +10 features on the next rebuild) emits the `f_rg_` family: trailing realized vol (short/long), the vol's OWN-history percentile (the volatility regime), trend drift (short/long), a Kaufman trend-efficiency, up/down bar breadth, trailing return, and the BTC market regime (return + above/below its EMA) — all causal and scale-invariant, the handoff's named contemporaneous indicators. The point is to condition ONE model on observable regime state (knowledge in the weights), not a switchboard. New `inputs/regime_conditioning.py` runs the ablation: same strong model WITH vs WITHOUT `f_rg_`, compared on TWO separate questions — cross-era STABILITY (eras-positive, worst-era, era-mean; does it generalize rather than memorize one regime) and after-cost EDGE — recomputing `f_rg_` from klines if the dataset predates the block so it runs before a rebuild. Writes `outputs/AA-evals/<date>/regime-conditioning-*.md`; wired into ch3 as the "Regime Conditioning (Part 2)" section (persisted record or bounded inline behind `RUN_INLINE_RG`). Validated on a BTC 4h dev slice: conditioning lifted AUC 0.606->0.628 and cross-era stability (eras-positive 1/5->3/5, era-mean -0.201%->-0.003%) but did NOT by itself cross the fee line (single-year after-cost +0.130%->-0.098%/trade as it traded more) — the honest shape: regime conditioning is a generalization fix, not a free edge. Full run: `.venv/bin/python inputs/regime_conditioning.py --interval 4h`. Next: rebuild the 4h (and 5m) datasets so `f_rg_` is native, then run the full-market ablation + edge diagnostics for the real verdict.
-   2026-08-16: Built and validated **TauricResearch/TradingAgents v0.3.1** (`/Volumes/PortableSSD/Github/TradingAgents`, own `.venv`, MacPorts python 3.12): full test suite 576 passed / 2 expected skips in 92s, ruff clean, live keyless dataflows verified on BTC (Yahoo OHLCV/RSI, StockTwits `BTC.X`, Reddit RSS, Polymarket, dated news), both asset-mode graphs compile (crypto drops fundamentals; Anthropic/`claude-fable-5` wiring works), decision-log round trip and loud `AuthenticationError` failure mode confirmed. No LLM API key exists on this machine, so no live agentic run yet. Verdict in `research/tradingagents-applicability-assessment.md`: keep repos separate, consume it as a pre-market analysis service (Principle 1 bull/bear write-up plus a candidate rating signal to grade on the after-fee scoreboard), never as an execution trigger. Next: operator provisions one LLM provider key and a run budget, then wire `inputs/ta_research.py`.
-   2026-08-16 (later): Wired `inputs/ta_research.py`, the pre-market TradingAgents committee wrapper (runs under the TradingAgents `.venv`; crypto mode, market+social+news, deep=`claude-sonnet-5`, quick=`claude-haiku-4-5`, `benchmark_ticker=BTC-USD`, 8-symbol cost guard, `--dry-run`). It appends ratings to `memory/research-log.md`, redirects the framework's decision/reflection log to `memory/ta-decisions.md`, and saves report trees under `outputs/ta-reports/`. Dry-run validated both ways (exit 1 + loud abort without a key, 19-node graph compile with one). Key goes in the TradingAgents repo's gitignored `.env` (seeded from `.env.example`), never in this repo. Awaiting the operator's Anthropic key for the first live run.
-   2026-08-16 (evening): Executed the three levers and stood up the full research-to-paper loop. Lever 3 verdict (new `outputs/AA-evals/2026-08-16/cross-sectional-regime-4h-20260816.md`): PARTIAL, the BTC-up gate lifts the best 4h top-third (`f_d1_st_up`) to -0.117%/trade OOS from the -0.382% ungated baseline, sign-stable, still short of zero; lever 1 (daily frame) is downloading with an auto-chained `dataset_1d_allmarket` build. Full 8-coin committee sweep: BTC/ETH/SUI/DOGE/NEAR Underweight, SOL Hold, PEPE Sell (Yahoo sub-cent rounding artifact, journaled), TON failed (Yahoo listing stale since 2026-06-15). `ta_research.py` hardened: Yahoo collision overrides (SUI20947/TON11419/PEPE24478), per-symbol crash isolation, incremental journal writes. Paper book live via `inputs/paper_trade.py`: $10k start, one 1% SOL mechanics-rehearsal position, stops armed, 10-minute marks. Docx workflow: operator hand-formatted the TOC, rewrote the intro, and added the repo-URL header in `00-trader-workflow.docx`; code styles set to 8pt in place via python-docx; intro + URL synced back into the notebook. RENDER POLICY: never re-render the docx blind, use the operator's docx as pandoc `--reference-doc` so headers/styles survive, and re-apply the 8pt style pass after any regeneration.
-   2026-08-17: Executed and closed the crypto edge search. Funding archive landed (189 symbols); the btc+funding gate re-ran clean: NOT CLEARED (-0.067%/trade at achievable 0.15% cost, gate open 47% vs breadth's 24%). All four operator recommendations implemented same-day: (1) daily frame: 669-pair archive completed, baseline `dataset_1d_allmarket` built, inherited 2-day label found DEGENERATE on daily bars (base rate 0.068), label swept to +3/-1 ATR over 20 days (all 12 cells still NO-GO for the time-series picker), swept-label gated matrix FRAGILE/NOT CLEARED (20-day holds inherit bear-year drift; coarser frames trade fee count against per-trade exposure; 4h remains closest at -0.047%); (2) fee engineering: `ACHIEVABLE_COST_PCT=0.15` constant, `trade_binance.fee_status()` measures the account's real maker/taker and BNB-burn each run, paper book charges 0.075%/side; (3) narrow book: `narrow_book.py` whitelist (positive OOS contributors intersected with live liquidity) enforced in `trade_binance.resolve_entries` (NARROW_BOOK=off bypass); (4) microstructure: opt-in `f_ms_` block (8 hourly-derived daily features, validated causal and scale-invariant) ranked on the swept 1d frame: NO selection edge, execution-timing use only. The one positive OOS cell (f_mst_dir under btc+breadth) was falsified by the new pre-registered kill harness `mst_gate_walkforward.py` (fold pass 33%/27% vs the 60% bar, 0/16 tradeable gate widths positive): KILLED on both frames; no open crypto candidate remains at 15-20bp fees. Backup workplan written (`tasks/workplan-alpaca-hires-2026-08-17.md`): Track A Alpaca equities, Track B 1m Binance execution study. README and notebook integration: Edge Levers section (3.13), Committee Layer appendix with mermaid diagram, 15 chapter figures, deduped notebook cells. numpy pinned <2.3 (numba/pandas-ta silently broke at 2.4; the failure mode is silent feature loss, not a crash).
-   2026-08-18: Track A executed A1-A5 in one day and produced the project's FIRST SURVIVES. A1 preflight (`alpaca_check.py`): Keychain keys, ACTIVE paper account ($100k), clock, SPY bars; SIP consolidated feed entitled. A2 (`alpaca_data.py`): 12,539 active US equities enumerated, 2,673 pass the 20M dollar-volume screen, max-history adjusted daily bars downloaded (5.2 GB, 2016+, gitignored). A3 (`build_dataset_equity.py`): equity 1d frame through the SHARED builder (SPY in the BTC seat with column names kept for tool compatibility; equity ATR band 1-8%; trading-calendar gap gate; float32 panel for the 8GB machine): 2,547 names, 5.0M rows, 96 features. A4: first-pass ranked signals cleared the 5bp cost bar OOS but were KILLED by the walk-forward harness on fold stability (selection 30%/45% vs 60%); beta plus survivorship can fake absolute positives, so the top-minus-market spread is the bar. The pre-registered CANONICAL test (`equity_momentum_monthly.py`, non-overlapping monthly holds, nothing picked on our scores): 12-1 momentum SURVIVES (spread +1.082%/month, t 2.53, abs 89% / sel 79% of folds; 6-1 and long-only low-vol killed exactly as the literature predicts, which validates the harness). Survivorship stress PASSES (edge GROWS to +1.586%/month in the top-500 liquidity tier where delisting bias is smallest; holds under adversarial delisting injection at the realistic cell, t 2.02; sits just above the CRSP anchor). Portfolio sim: 30.5% CAGR net of measured turnover (28%/month) vs SPY 17.5%, at 35% vol, Sharpe 0.93 below SPY's 1.02, worst month -27.8% = July 2026 (a live crash, disclosed before the deployment decision). Operator approved paper deployment: A5 `alpaca_trade.py` live on the PAPER account: 50 names at 1.8%, never-short and never-margin in code, 5% cap, 10% floor, 3% daily circuit, PDT-safe; the charter's 3/week cap and -7% stop are off-by-default switches for the systematic basket (journaled: they would forbid the rebalance and amputate the factor); 25% catastrophe stop added (`check` command). First rebalance filled 50/50 orders; measured slippage 3.8bp/side (`alpaca_execution_report.py`), inside the 5-10bp model. Weekly cadence adopted after `equity_weekly_factors.py` showed 12-1 SURVIVES weekly holds (+0.259%/week, t 2.47, sel 85% vs monthly's 79%; 5-day reversal killed, 21-day marginal); 5-day cadence guard and book state in `memory/alpaca-book-state.json`. Weekly runbook: data download, rebalance --execute, check, status, execution report. Next rebalance Mon 2026-08-24; execution verdict ~late September on ~6 cycles. Crypto paper book unchanged ($10k, SOL rehearsal). Track B (1m execution study) pending.
-   2026-08-27 (dashboard rebuild): Expanded the Quarto dashboard from one page of five panels to six pages of thirty, and put it in dark mode (`darkly` plus `dashboard/dashboard.scss`, one palette shared by the stylesheet and every chart). `inputs/dashboard_data.py` gained ten emitters feeding them: `orders_history` (every order the broker saw, filled or not, with submit and fill stamps and seconds-to-fill), `roundtrips` (FIFO lot matching, so a finished trade reports what it actually made), `timeline` (fills, rebalances, report writes and schedule wake-ups in one time-ordered stream), `sector_map`/`mixes` (four portfolio cuts for the pie charts; there is no GICS table in this repo, so a holding's sector is the sector ETF whose daily moves it followed most closely over 250 days, and the page says so), `attribution` (each holding's effect on the whole account in percentage points, not its own return), `markets` (adjusted daily OHLCV, MACD 12/26/9, RSI 14 and EMA 20/50/200 for SPY/QQQ/IWM/GLD/TLT, all eleven sector funds, and the four holdings moving the account most), `risk_series` (drawdown, daily moves, rolling volatility), `report_log` (every `outputs/AA-evals/*/DAILY-*.md` with the equity it claimed) and `archive` (a stamped snapshot per render under `outputs/dashboard/history/`, capped at 400, so the page has a memory of itself). Pages: Money, What we own, Trades, Markets, Rules, History. **Two real bugs found and fixed while building it.** (1) `_api_bars` fetched Alpaca bars without `adjustment=ALL`; the SPDR sector funds split on 2025-12-05, so the raw series halved overnight and the page would have printed technology as down 28% over a year when it was up 44%. The on-disk store in `alpaca_data.py` already downloaded adjusted, which is why nothing else had caught it. (2) SPY's bar for 2026-02-02 carries a low of 69.005 on a day that opened at 689.58, a misplaced decimal in the feed; one such bar drags a candle chart's axis to zero. `_clip_bad_prints` pulls a day's range back to its own open and close when the low is under half the body or the high over double it, never touches the close, and reports the count so the chart says it happened. Layout notes for anyone editing the page: row heights must sum to 100 per page or the first row eats the viewport, and `coord_polar` forces a square panel so the drawing does not fill its device, which showed as white columns beside every pie until `dev.args = list(bg = card)` was set. Render is unchanged: `./scripts/render_dashboard.sh [--open]`, about 55 seconds end to end, one self-contained ~10 MB `dashboard/dashboard.html`.
-   2026-08-28 (analysis pages): The dashboard was showing the book but none of the analysis, and the operator said so: the workflow's figures took three months to build and appeared nowhere. Two new pages, and the fix was not to paste the old PNGs in. **Analysis** runs the lab engines on the market we actually trade. `inputs/analysis_charts.py` imports `outputs/1A-macd/macd.py`, `outputs/1B-confluence/confluence.py`, `outputs/1C-fibonacci/fib.py` and their `build_*` figure modules **by file path and calls them unchanged** on Alpaca daily bars for 66 symbols (5 index funds, 11 sector funds, all 50 holdings), because those engines only ever wanted an OHLCV frame and never cared that it came from ccxt. It emits: the three interactive dashboards with their symbol dropdowns intact (`dashboard/analysis/{macd,confluence,fibonacci}.html`, plotly.js written once beside them via `include_plotlyjs="directory"`), a divergence matrix across the universe, the four-gate screen re-run on equities with a Corwin-Schultz spread estimate, and a per-symbol signal-journal study sheet (candles, MACD, confluence score, Fibonacci levels, and a marker at every guarded signal and divergence). **Research** is the archive: `inputs/research_figures.py` parses the README for every `![](...)` with the heading and caption the README itself gives it, writes a 1100px copy (cached on mtime, so it is not redrawing 23 PNGs every twenty minutes), and the page renders them as a contact sheet stating plainly that they are crypto figures for a strategy that was killed, and that this is why the book trades equities. **Three traps worth remembering.** (1) The MACD engine's `strength` is zero on every bar and set only on the bar a guarded signal fires, so reading the last bar returns zero for every name that did not trade a signal today; the matrix reports the most recent signal and how many bars ago instead. (2) Pandoc's `--embed-resources` resolves `src`, `href` **and `data-src`** alike, so an iframe pointing at a 7 MB analysis page gets inlined whole: the dashboard went 10 MB -> 40 MB. The address now lives in `data-page` and a small script maps it to a URL in the browser, which pandoc leaves alone; page is 16 MB. (3) An absolute path inside a markdown image is read as root-relative and pandoc hunts for `./Volumes/...`; figure paths are relative to `dashboard/`. Render is unchanged, `./scripts/render_dashboard.sh`, now about 2 minutes; the generated feed (`outputs/dashboard/analysis.json`, `figures.json`, `figures/`, `dashboard/analysis/`) is gitignored and rebuilt from committed code.
-   2026-08-28 (fixed-frame layout): Operator instruction, standing: **the dashboard must not scroll**, and where a page has room for three rows it is split into three rows of up to five columns. `scrolling: false`, and every page rebuilt to exactly three rows whose heights sum to 100. Panel count went 65 -> 88, with new ones written to fill the grid rather than pad it: cash against its floor, book-minus-market by day, where the move sits against the plan's expected range, a rules-at-a-glance tally, how evenly the money is spread, winners and losers by their own move, money in and out by day, turnover per rebalance, win and loss sizes, every industry past month, where the money is hiding (shares against gold and bonds), confluence agreement across the universe, how much room is left before each rule fires, and which rules have been unhappy and when. Valueboxes were added to the five pages that had none. **The layout lessons, all of them earned the hard way.** (1) Row heights must sum to 100 per page or the first row swallows the viewport and everything under it is cut. (2) Quarto's dashboard format draws every figure at eight inches by five and lets the card shrink it to fit; neither the format block, the `knitr:` block, nor `opts_chunk$set` overrides that, and a per-chunk `fig-width` only changes the shape, not the shrink. So the text is scaled up in the theme instead (base 26, axis 21, subtitle 22) and each figure is sized to its own card, `fig-width = card_width / 34.5` capped at 20 inches with the height following the card's aspect, so the shrink is the same everywhere and one theme size means one size on screen. The figures knitted as child documents (the candle tabs and the journal sheets) are sized the same way in their `knit_child` text, and their subtitles were dropped because in a wide short card three stacked explanations leave a strip of chart; the explanation moved to the card beside them. (3) A duplicated chunk option is a **YAML error that leaves the previous `dashboard.html` on disk**, so `quarto render --quiet | grep ERROR` reports nothing and the next screenshot is of a stale file. Always check the exit code and the file's mtime. **Research is the one page that still scrolls**, inside a single `.scrolling` column: 23 archive figures cannot be made to fit a screen, and shrinking them to where they do would destroy the only thing they are for.
-   2026-08-28 (headline row and research sheet): Two operator corrections on the fixed-frame layout. **The top row was shouting**: bslib sizes a value box's number to the box, so on a tall box a five-figure sum arrived at forty pixels and the row read as a billboard with a dashboard underneath. The value-box type is now fixed rather than scaled (value 1.15rem, title 0.68rem, icon 1.05rem, all `!important` because bslib's own rule is more specific than a plain class selector), and the headline row shrank from 14% to 9% on every page, with the space going to the charts. **The Research page now scrolls freely and shows its figures full size.** It had been a grid of cards two to a row inside a `.scrolling` column, which squeezed 23 figures into slivers. The cause was Quarto's fill container: it makes every child of a cell output a flex item and caps images at the height of the box. The contact sheet is now plain HTML in a `.research-sheet` div that opts out of that (`display:block`, `overflow-y:auto`, images at `max-height:none` and `object-fit:unset`), one figure per full-width row at the size it was drawn for. Text-only cards also gained `overflow-y:auto` (scoped with `:not(:has(img))` so a chart card keeps its fill-to-card sizing) because a card of prose in a fixed-height page clips its last line rather than admitting there is more.
